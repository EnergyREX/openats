info:
  name: Get models
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/api/v1/ai/models"
  auth: inherit

runtime:
  variables:
    - name: baseURL
      value: http://localhost:6500

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
