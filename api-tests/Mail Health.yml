info:
  name: Mail Health
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/v1/health/mail"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
