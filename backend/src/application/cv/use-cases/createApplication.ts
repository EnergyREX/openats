export async function createApplication(candidateUuid: string, offerUuid: string) {
    // Compare with the candidate with the offer.

    // Get related data, such as  

    // Pass through LLM
        // - It must generate: an score, annotations via llm.
}